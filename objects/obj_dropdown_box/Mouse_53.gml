/// @description Select
if (input_box_dd_Global_Left_Pressed()) {//unselected so now act on the selection:
	input_box_dd_apply(useCase);//edit this function with what should happen for each selection
}