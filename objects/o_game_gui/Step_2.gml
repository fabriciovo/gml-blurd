var ww = window_get_width();
var hh = window_get_height();

if (ww != old_window_width || hh != old_window_height) {
    old_window_width = ww;
    old_window_height = hh;
	instance_destroy(o_button_shop_item)
	if shop_open  {
		event_user(0)
	} 
 
}